#!/bin/sh
set -eu

# Authme is the upstream Tauri/WebKitGTK binary staged by apply_extra at
# /app/extra/authme. The tray support libraries are supplied by the ayatana
# module in the manifest.
export WEBKIT_DISABLE_DMABUF_RENDERER=1

exec /app/extra/authme "$@"
